# Asserts, from outside the release pipeline, that what consumers can install is what was merged.
#
# Issues #1 and #2. The point is stated in #1: a check inside the pipeline cannot detect the absence
# of the pipeline. A guard is a stage, a stage only evaluates inside a run, and the failure that
# started this was a merge that queued no run at all. A schedule fires whether or not CI triggered,
# which is the property that matters.
#
# The alert IS this workflow failing. There is no Slack, webhook or mail anywhere in either repo, and
# a red scheduled run is a channel that already exists and is already watched. Building a notifier
# would have been a larger job than the check.
name: Delivery assertion

on:
  schedule:
    - cron: '17 * * * *'
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  stalled-gate:
    name: No recent run parked awaiting approval
    runs-on: ubuntu-latest
    steps:
      - name: Fail if a recent run has been waiting over an hour
        env:
          GH_TOKEN: ${{ github.token }}
          THRESHOLD_MINUTES: '60'
          # Only recent stalls are actionable. The first real run of this check found 27 parked runs
          # going back to 2026-08-09; failing on those forever would leave this permanently red,
          # which is the same "nobody notices" failure #2 is about wearing a different hat. Old
          # parked runs want cancelling once, not alerting on every hour.
          LOOKBACK_DAYS: '7'
        run: |
          set -euo pipefail

          # `waiting` is a run parked on a protected environment's required reviewer. It reports no
          # failure, notifies nobody, and is visually indistinguishable from a slow run - which is
          # how four master merges once sat unapproved for 17 days while nuget.org stayed on 1.0.4.
          gh api "repos/${GITHUB_REPOSITORY}/actions/runs?status=waiting&per_page=100" \
            --jq '.workflow_runs[] | [.id, .created_at, .head_branch, .html_url] | @tsv' > waiting.tsv

          if [ ! -s waiting.tsv ]; then
            echo "No runs awaiting approval."
            exit 0
          fi

          now=$(date -u +%s)
          stalled=0
          stale=0

          while IFS=$'\t' read -r id created branch url; do
            [ -z "${id:-}" ] && continue
            minutes=$(( (now - $(date -u -d "$created" +%s)) / 60 ))

            if [ "$minutes" -gt $(( LOOKBACK_DAYS * 1440 )) ]; then
              stale=$(( stale + 1 ))
              continue
            fi

            if [ "$minutes" -gt "$THRESHOLD_MINUTES" ]; then
              echo "::error::Run $id ($branch) has been awaiting approval for ${minutes}m - $url"
              stalled=1
            else
              echo "Run $id ($branch) waiting ${minutes}m - under threshold."
            fi
          done < waiting.tsv

          # Reported, never failed on. Whether master actually shipped is answered authoritatively by
          # the commit comparison in the other job - and because a release publishes everything up to
          # it, an old parked run usually means stale bookkeeping rather than unpublished code.
          if [ "$stale" -gt 0 ]; then
            echo "::notice::$stale run(s) have been waiting longer than ${LOOKBACK_DAYS}d and are not failed on here. Cancel them to clear the noise; whether master shipped is answered by the nuget.org comparison."
          fi

          exit "$stalled"

  published-matches-master:
    name: nuget.org matches origin/master
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Compare the published package's source commit with master
        env:
          PACKAGE: quilt4net.toolkit
          # A publish is not instant, and a merge must not alarm while its release is still running.
          GRACE_MINUTES: '90'
        run: |
          set -euo pipefail

          master=$(git rev-parse origin/master)
          merged_at=$(git show -s --format=%ct "$master")
          age=$(( ( $(date -u +%s) - merged_at ) / 60 ))

          # SourceLink stamps the producing commit into the nuspec, so this compares commits rather
          # than guessing from version numbers - a version says nothing about what is in it.
          latest=$(curl -sf "https://api.nuget.org/v3-flatcontainer/${PACKAGE}/index.json" \
                     | jq -r '.versions[-1]')
          curl -sfL -o pkg.nupkg \
            "https://api.nuget.org/v3-flatcontainer/${PACKAGE}/${latest}/${PACKAGE}.${latest}.nupkg"
          unzip -p pkg.nupkg "*.nuspec" > pkg.nuspec
          published=$(grep -o 'commit="[0-9a-f]*"' pkg.nuspec | head -1 | cut -d'"' -f2)

          echo "origin/master        : $master (${age}m old)"
          echo "$PACKAGE $latest : $published"

          if [ "$master" = "$published" ]; then
            echo "Published package was built from master."
            exit 0
          fi

          # An ancestor means master moved on and nothing shipped it. A non-ancestor means something
          # stranger - a package built from a branch, or a force-push - and is worth its own message.
          if git merge-base --is-ancestor "$published" "$master" 2>/dev/null; then
            behind=$(git rev-list --count "$published..$master")
            if [ "$age" -lt "$GRACE_MINUTES" ]; then
              echo "master is $behind commit(s) ahead but only ${age}m old - within the grace period."
              exit 0
            fi
            echo "::error::nuget.org is $behind commit(s) behind master, and master has been there for ${age}m. The release did not ship."
            exit 1
          fi

          echo "::error::The published package was built from $published, which is not an ancestor of master."
          exit 1
