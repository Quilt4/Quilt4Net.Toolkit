# Mission: Quilt4Net.Toolkit

Health endpoints, API logging, remote configuration, and content management, published as NuGet packages.

## Documentation Requests
- **IComponentService / AddComponentService<T> documentation** — The Health README mentions `AddComponentService<T>()` but lacks details. Add documentation covering: the `IComponentService` interface, how to implement `GetComponents()`, `Component` and `CheckResult` models, `Essential` flag behavior (true = Unhealthy, false = Degraded), DI support in component services, and a real-world example. Requested by: Tharga.Starter (https://github.com/Tharga/Starter).
